<!-- ⚡ AuthMatrix Azure API Management (APIM) Policy Template -->
<policies>
    <inbound>
        <base />
        
        <!-- 1. Strip untrusted external client headers -->
        <set-header name="X-User-Id" exists-action="delete" />
        <set-header name="X-User-Roles" exists-action="delete" />
        <set-header name="X-User-Permissions" exists-action="delete" />

        <!-- 2. Edge JWT Signature & Claim Validation (Okta / Entra ID) -->
        <validate-jwt header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized: Invalid Bearer token" require-scheme="Bearer">
            <openid-config url="https://login.microsoftonline.com/{{ENTRA_TENANT_ID}}/v2.0/.well-known/openid-configuration" />
            <required-claims>
                <claim name="aud" match="any">
                    <value>https://api.authmatrix.local</value>
                </claim>
            </required-claims>
        </validate-jwt>

        <!-- 3. Extract Claims & Inject Trusted Internal Headers -->
        <set-variable name="user-id" value="@(context.Request.Headers.GetValueOrDefault("Authorization","").AsJwt()?.Subject)" />
        <set-variable name="user-roles" value="@(string.Join(",", context.Request.Headers.GetValueOrDefault("Authorization","").AsJwt()?.Claims.GetValueOrDefault("roles", new string[0])))" />

        <set-header name="X-User-Id" exists-action="override">
            <value>@(context.Variables.GetValueOrDefault<string>("user-id"))</value>
        </set-header>
        <set-header name="X-User-Roles" exists-action="override">
            <value>@(context.Variables.GetValueOrDefault<string>("user-roles"))</value>
        </set-header>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
